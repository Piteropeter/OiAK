#pragma once
#include <fstream>
#include <iostream>
#include <vector>
#include <sstream>
#include <cmath>

namespace oiak {

template <std::size_t base>
struct BigInteger {



//public:
	//template <std::size_t base>
    BigInteger() = default;
    BigInteger(int value);

};

template <std::size_t exponent, std::size_t siginificand>
class BigDecimal {



};

struct Test {

};

} // namespace oiak
